imports:
    - { resource: config.yml }

#doctrine:
#    orm:
#        metadata_cache_driver: apc
#        result_cache_driver: apc
#        query_cache_driver: apc

monolog:
    handlers:
        main:
            type:         fingers_crossed
            action_level: error
            handler:      nested
        nested:
            type:  stream
            path:  "%kernel.logs_dir%/%kernel.environment%.log"
            level: debug

#IceMercuryClient configuration
ice_mercury_client:
    base_url: https://mercury.ice.cam.ac.uk/api/

#IceJanusClient configuration
ice_janus_client:
    base_url: https://janus.ice.cam.ac.uk/api/

#IceVeritasClient configuration
ice_veritas_client:
    base_url: https://veritas.ice.cam.ac.uk/api/